<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.sandra.proyecto0509">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Resul_Jefe_Estudios"></activity>
        <activity android:name=".Pantalla_Secundaria_Jefe" />
        <activity
            android:name=".RegistrarUsuarios"
            android:label="@string/title_activity_registrar_usuarios"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CrearUsuarios"
            android:label="@string/title_activity_crear_usuarios"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Resultados"
            android:label="@string/title_activity_resultados"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PantallaPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".PantallaSecundaria" />

        <service android:name=".Servicio" />
    </application>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

</manifest>